let browsers = ["chrome", "edge", "safari "];
browsers.push("opera")
// console.log(browsers)
// console.log(browsers.length)
//browsers.pop();
for(let i = 0 ; i< browsers.length; i++)
{
    console.log(browsers[i]);
    if(browsers[i] === "opera")
    {
        console.log("Opera browser avaialble ");
    }
        
}